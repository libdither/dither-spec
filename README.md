# dither-spec (archived)

> **Moved:** the Dither spec now lives in the [libdither/libdither](https://github.com/libdither/libdither) monorepo at [`research/`](https://github.com/libdither/libdither/tree/main/research), with this repo's full history merged in. It is still rendered at <https://dither.link/docs/>. This repo is archived and read-only.
